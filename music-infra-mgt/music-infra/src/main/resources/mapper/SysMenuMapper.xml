<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.music.player.infra.mapper.SysMenuMapper">


    <select id="querySysMenu" parameterType="com.music.player.infra.api.dto.QuerySysMenuDto"
            resultType="com.music.player.infra.api.vo.QuerySysMenuVo">
        select id from sys_menu
        <where>
            del_flag = '0'
            <if test="">
                and
            </if>
        </where>
    </select>

</mapper>